import { View, Text } from "react-native"

export default function AppShell() {
    return (
        <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
            <Text>AppShell ok</Text>
        </View>
    );
}